# === RPC & Network ===
# MONAD testnet RPC (contoh)
RPC_URL=https://testnet-rpc.monad.xyz
# Opsional: isikan CHAIN_ID untuk verifikasi; kosongkan jika tidak yakin
CHAIN_ID=

# === Contract & ABI ===
CONTRACT_ADDRESS=0xYourContractOnMonadTestnet
MINT_FUNC=mint
# OPSIONAL: JSON ABI override (array). Kosongkan jika fungsi default sudah sesuai.
# Contoh: ["function mint(uint256 _count) payable"]
ABI_OVERRIDE=

# === Mint Params ===
# Harga mint per NFT dalam Ether, bukan wei
MINT_PRICE=0.01
MINT_AMOUNT=1
# OPSIONAL: Argumen fungsi mint dalam JSON array. Default: [MINT_AMOUNT]
# Contoh: [1, "0xproof...", ["0xproof1","0xproof2"]]
MINT_ARGS=

# === Gas & Fee ===
# Set salah satu mode fee:
# - Legacy: GAS_PRICE_GWEI
# - EIP-1559: MAX_FEE_GWEI + MAX_PRIORITY_GWEI
# - Atau kosongkan untuk auto
GAS_LIMIT=350000
GAS_PRICE_GWEI=
MAX_FEE_GWEI=
MAX_PRIORITY_GWEI=

# === Mode eksekusi ===
# simple/single: 1 wallet; multi: banyak wallet
MODE=single
PRIVATE_KEY=0xYourTestnetPrivateKey
PRIVATE_KEYS=0xKey1,0xKey2

# Delay antar wallet (ms) dan paralelisme (multi mode)
TX_DELAY_MS=2000
PARALLEL=1

# === Retry & Bump ===
RETRY_ATTEMPTS=5
RETRY_BACKOFF_MS=2000
RETRY_BACKOFF_MULTIPLIER=1.6
GAS_BUMP_PERCENT=15

# === Debug & Simulasi ===
DEBUG=1
DRY_RUN=1
